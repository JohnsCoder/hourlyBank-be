{
  "name": "hourlybank-be",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "tsnd --watch --transpile-only --rs server.ts",
    "secret": "cd src/ssh && ssh-keygen -t rsa -b 4096 -m PEM -f jwtRS256.key -P \"\" && cd ../..",
    "prisma": "prisma generate && prisma db push"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "prisma": "^4.10.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@prisma/client": "^4.10.1",
    "@types/jsonwebtoken": "^9.0.1",
    "apollo-server": "^3.11.1",
    "dotenv": "^16.0.3",
    "graphql": "^16.6.0",
    "jsonwebtoken": "^9.0.0"
  }
}